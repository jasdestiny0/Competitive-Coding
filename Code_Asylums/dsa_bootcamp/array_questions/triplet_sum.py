def triple_sum(arr, num):
    closestDiff=float("inf")
    soln=None
    pairs={}
    for i in range(0, len(num)-1, 1):
        for j in range(i+1, len(num, 1)):
            if num-arr[j] in pairs and abs(num-arr[j]:
            pairs.add(arr[i]+arr[j])